module Lecture where

import qualified Data.Monoid as M

-- Monoids
-- instance Monoid [a] where
--    mempty = []
--    mappend = (++)

-- Since base-4.9.0.0, Semigroup has been introduced.
-- Since base-4.11.0.0 (GHC 8.4), Semigroup is a superclass of Monoid.
